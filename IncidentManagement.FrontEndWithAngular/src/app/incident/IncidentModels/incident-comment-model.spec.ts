import { IncidentCommentModel } from './incident-comment-model';

describe('IncidentCommentModel', () => {
  it('should create an instance', () => {
    expect(new IncidentCommentModel()).toBeTruthy();
  });
});
