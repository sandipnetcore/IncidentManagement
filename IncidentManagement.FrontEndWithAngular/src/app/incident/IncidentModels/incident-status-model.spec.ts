import { IncidentStatusModel } from './incident-status-model';

describe('IncidentStatusModel', () => {
  it('should create an instance', () => {
    expect(new IncidentStatusModel()).toBeTruthy();
  });
});
