import { IncidentDetailModel } from './incident-detail-model';

describe('IncidentDetailModel', () => {
  it('should create an instance', () => {
    expect(new IncidentDetailModel()).toBeTruthy();
  });
});
