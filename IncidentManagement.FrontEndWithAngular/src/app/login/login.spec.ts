import { Login } from './login';

describe('Login', () => {
  it('should create an instance', () => {
    expect(new Login()).toBeTruthy();
  });

  //it('value should be emppty for the properties of login class', () => {
  //  var obj = new Login();
  //  expect(new Login()).toBeTruthy();
  //  expect(obj.userName).toBeDefined();
  //});
});
